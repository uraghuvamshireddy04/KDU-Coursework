Homework 9

Using Rest API we need to call different API if we want different data or else get whole data in a single API if though if we dont want it.
This can be solved using GraphQL where only the data can be fetched in the overfall information and which mutating data we can get in return the data directly without another API call